# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Node (app/web)
node_modules/
.next/
out/

# Local config and secrets -- never committed
.env
.env.local
*.pem
*.key

# OS
.DS_Store
Thumbs.db

# Ledger (file-backed until persistence lands)
evidence/ledger/

# PreToolUse cross-call fragment window (temp dir, but never commit one)
zerotrace-window/

# The demo corpus is generated (demo/corpus/generate.py), never committed: it is a
# tree of identifier-shaped records, and checking it in is the exact thing ZeroTrace
# refuses to let anyone do.
demo/corpus/bharat-digital/
