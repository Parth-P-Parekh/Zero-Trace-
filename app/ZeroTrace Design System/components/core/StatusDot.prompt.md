# StatusDot

\`\`\`jsx
<StatusDot state="clean" />
<StatusDot state="blocked" size={8} />
<StatusDot state="ink" live />
\`\`\`

Never grow past 10px — signal colour is a signal, not a surface. Always pair with a word; the dot alone is not the label.
