# Radio

\`\`\`jsx
<Radio name="mode" value="redact" checked label="Redact and dispatch" hint="Default. The model never sees the value." />
<Radio name="mode" value="block" label="Block the request" />
\`\`\`

Use for three or more mutually exclusive options with explanations; two short options are a \`SegmentedControl\`.
