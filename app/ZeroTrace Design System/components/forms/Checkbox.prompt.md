# Checkbox

\`\`\`jsx
<Checkbox label="Log redacted values (hashed)" checked={on} onChange={fn} />
<Checkbox label="Block on failure" hint="Payload is not dispatched if the sweep errors." />
\`\`\`

Label states the effect, not the setting name. Disabled means ramp .36, never a lighter fill.
